# Mock app for the swimmerplot module

This function creates a standalone Shiny application that demonstrates
the swimmerplot module with example data from the pharmaversesdtm
package.

## Usage

``` r
mock_swimmerplot()
```

## Examples

``` r
if (FALSE) { # \dontrun{
mock_swimmerplot()
} # }
```
