# Run an example swimmer plot module in the module manager

Launches an example app that shows a swimmer plot module integrated in
the module manager surface. Displays data prepared from the
pharmaversesdtm package.

## Usage

``` r
mock_swimmerplot_mm()
```

## Examples

``` r
if (FALSE) { # \dontrun{
mock_swimmerplot_mm()
} # }
```
